import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  starContainer: {
    marginTop: 5,
    flexDirection: 'row',
  },
  star: {
    color: '#ffac30',
    fontSize: 14,
  },
});

export default styles;
